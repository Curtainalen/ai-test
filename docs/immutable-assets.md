# 不可删除资产规则

项目与用户均为不可删除资产。产品只支持创建、查询、更新与停用；停用用户用于撤销登录权限，不等同于删除。

移除资产的唯一合法途径是未来经过审批的数据清理流程，该流程不属于当前产品功能，也不由普通 API 或前端页面提供。

开发红线：禁止为 `projects` 或 `users` 注册 `DELETE` 路由；禁止在项目或用户相关前端页面提供删除入口。任何下线需求必须独立评审，并同时更新 API 契约、权限设计、数据保留策略和回归测试。
